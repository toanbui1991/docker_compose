**Note about docker, docker-compose and airflow:**
- docker-compose down -v. to remove all containers and volumnes related to docker-compose.
- all value in .env is treat as string. do not use '' to present string.